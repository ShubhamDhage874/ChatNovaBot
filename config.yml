language: en

pipeline:
- name: WhitespaceTokenizer
- name: RegexFeaturizer
- name: LexicalSyntacticFeaturizer
- name: CountVectorsFeaturizer
- name: CountVectorsFeaturizer
  analyzer: char_wb
  min_ngram: 1
  max_ngram: 4
- name: DIETClassifier
  epochs: 30              # 🔹 कमी केले (100 → 30)
  constrain_similarities: true
  batch_size: 16          # 🔹 कमी batch (RAM बचत)
  embedding_dimension: 20 # 🔹 हलके embeddings
- name: EntitySynonymMapper
- name: ResponseSelector
  epochs: 30              # 🔹 कमी केले (100 → 30)

policies:
- name: MemoizationPolicy
- name: RulePolicy
- name: TEDPolicy
  max_history: 5
  epochs: 30              # 🔹 कमी केले (100 → 30)

assistant_id: 20250904-004210-relaxed-relish
